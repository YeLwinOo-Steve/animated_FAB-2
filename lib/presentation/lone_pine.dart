import 'package:flutter/material.dart';

class LonePine extends StatelessWidget {
  const LonePine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
