import 'package:flutter/material.dart';

class OneChange extends StatelessWidget {
  const OneChange({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
