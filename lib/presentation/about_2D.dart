import 'package:flutter/material.dart';

class About2D extends StatelessWidget {
  const About2D({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
