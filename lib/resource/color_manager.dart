import 'package:flutter/material.dart';

class ColorManager {
  static const primary = Color.fromRGBO(80, 182, 0, 100);
  static const red = Color.fromRGBO(218, 28, 28, 100);
  static const white = Color.fromARGB(255, 255, 255, 255);
  static const black = Color.fromRGBO(0, 0, 0, 100);
}
